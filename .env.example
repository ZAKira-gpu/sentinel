# ──────────────────────────────────────────────────────────────
# Elastic Sentinel — Environment Variables
# Copy this file to .env and fill in your values.
# ──────────────────────────────────────────────────────────────

# ── Elastic Serverless (preferred) ───────────────────────────
# Your endpoint looks like:
#   https://PROJECT_ID.es.europe-west1.gcp.elastic-cloud.com
ELASTIC_URL=https://sentinel-d31f06.es.europe-west2.gcp.elastic.cloud:443
ELASTIC_API_KEY=WFloOGU1d0ItUGVNekI1cXZDNk46b3ZPTEVJaEZmRUIzTWNLMHB5SFpTUQ

# ── Local Elasticsearch (alternative) ───────────────────────
# If running locally, set ELASTIC_URL=http://localhost:9200
# and leave ELASTIC_API_KEY blank.

# ── LLM Provider (for agent reasoning) ────────────────────────
# Choose one:
OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ── Agent Settings ─────────────────────────────────────────────
ANOMALY_THRESHOLD=2.0       # spike_ratio above which an incident is raised
AGENT_MAX_ITERATIONS=8      # ReAct loop iteration limit
LOG_WINDOW_MINUTES=60       # how many minutes of logs to simulate
